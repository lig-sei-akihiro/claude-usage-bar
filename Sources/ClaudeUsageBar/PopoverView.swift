import SwiftUI
import ClaudeUsageBarCore

/// The dropdown content shown when the status item is clicked: one row per account
/// with session/weekly bars and reset times (requirement #5 — multi-account).
/// Implemented by the App-UI agent.
struct PopoverView: View {
    var body: some View {
        Text("TODO")
    }
}
